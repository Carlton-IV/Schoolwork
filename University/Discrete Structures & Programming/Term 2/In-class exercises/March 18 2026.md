1. `Find the maximum distance between two occurances of the same element in an array in O(nlogn) time. For example: InputArray = [ 1,2,3,1,2,4,5,6,2,3 ] Maxdistance = 8 - 1 = 7 (from 2 to 2)`

# My answer:
make a bucket of the size of the largest number in the array in O(n), then assign each number's index to the matching key using chaining. Find the largest distance in any of the keys. Mine runs in O(n)

# Given Answer:
Sort the array using HeapSort in O(nlogn). Make a bucket to fit the numbers. Define h(x) = position of x in the array. Use chaining. Find the largest distance in any of the keys
